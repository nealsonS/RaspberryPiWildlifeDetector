[project]
name = "raspberrypiwildlifedetector"
version = "0.1.0"
description = "Raspberry Pi "
readme = "README.md"
requires-python = ">=3.13.5"
dependencies = ["numpy", "opencv-python", "picamera2", "python-dotenv"]
